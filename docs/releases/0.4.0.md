# Vocal-More 0.4.0

本版本加入独立的指令模式。自定义指令快捷键（可配置为 F13）现在可以独立于听写快捷键配置和触发，指令模式仅支持四个 Qwen3.5 Omni 模型，并针对 Ghostty 与 Terminal 场景做了适配。

指令模式使用独立的语音指令 Prompt。模型判断需要时可以联网搜索；终端场景会限制为安全的纯命令输出，避免把解释性内容混入命令。指令执行失败时不会粘贴部分结果，搜索会话结束后会复位状态，减少后续指令受到上一轮搜索上下文影响的风险。

设置界面、浮动胶囊和历史记录同步更新，以展示指令模式的快捷键、状态与结果。
