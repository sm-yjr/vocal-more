# Vocal-More 0.3.26

本版本新增 Fun-ASR Realtime 支持，接入 DashScope Recognition 的 provider-native 实时链路。它复用现有 PCM 流式发送与尾音 flush 机制，减少结束录音时的截断风险。

当 provider 返回空结果或请求失败时，自动回退到 `qwen3-asr-flash`，保持可用的识别结果。北京区域的 Fun-ASR Realtime 费用按官方价格 `¥0.00033/秒` 展示。
