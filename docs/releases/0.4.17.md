# Vocal More 0.4.17

本版本修复 macOS 启动时 Sparkle 加载引起的额外内存占用。加载更新框架时关闭 PyObjC 的全量类扫描，改为按需获取更新控制器，保留自动更新与手动检查更新的原有行为。

在本机同一 0.4.16 应用包的修正前后对照中，启动 60 秒时主进程 physical footprint 从 307.5 MiB 降至 93.8 MiB，减少约 69.5%。这是一次未主动录音、未打开设置页的启动测量；不同设备、系统及使用场景的内存占用会有所不同。

GPL-3.0-only 许可保持不变。
