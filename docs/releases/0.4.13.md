# Vocal More 0.4.13

本版本让原生波形胶囊按当前文本实际占用的行数调整展开高度。AppKit 使用与文本标签相同的字体和宽度测量换行结果，短文本保持紧凑，换行内容逐步展开，长文本达到 200pt 上限后在容器内滚动；文本变短或清空时窗口也会收回到对应高度。

展开状态下，波形会根据两侧按钮、Prompt 标签和剩余内容区域动态增加 bar，最多显示 80 个，hands-free 展开宽度下能够填充可用空间。文本测量、上限滚动、波形跨度、初始 Prompt 提示、收回和多语言布局均加入真实 AppKit 回归检查。

GPL-3.0-only 许可保持不变。
