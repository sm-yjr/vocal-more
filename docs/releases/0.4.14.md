# Vocal More 0.4.14

本版本改进实时语音连接失败时的重试和反馈。连接会按 1、2、4、8、16 秒的退避序列重试，取消录音会立即中断等待；重试耗尽后会把明确的连接错误传递给界面，不再静默切换到隐藏的批量请求。

浮动胶囊会显示连接中、重试和失败状态，并保留最后一条转写内容；用户可以从失败提示恢复到可用状态。DNS、TLS 和鉴权错误会保留为诊断信息，同时避免输出凭据等敏感内容。模式切换、停止录音和预热会话也补充了并发取消与观察者清理，运行时模型文档和真实 AppKit 检查同步更新。

GPL-3.0-only 许可保持不变。
