## Qwen Audio 3.0 Realtime Plus 原生实时链路

- 新增 `qwen-audio-3.0-realtime-plus` 模型支持。
- 无论润色开关状态，均使用模型原生实时会话与 `response.create` 返回文本；关闭输入音频转写旁路，不使用 Fun-ASR 或 legacy fallback。
- 补齐原生实时文本事件处理、会话复用策略、计费信息和测试覆盖。

## 验证

- macOS 真实请求验证：润色开启和关闭均成功返回非空原生实时结果，未产生或消费 `input_audio_transcription` 事件。
- Python 全量测试：833 项通过。
